export class Stat {
  title: string;
  icon: string;
  description: string;

  constructor(title: string, icon: string, description: string) {
    this.title = title;
    this.icon = icon;
    this.description = description;
  }
}
